## Cube-Sticky 吸顶组件

<card>

### 介绍

吸顶组件，用于实现滚动时元素固定在顶部的效果。支持多个吸顶元素，当滚动到特定位置时会自动吸顶固定。

</card>

## 示例

<card>

### 基础用法

使用 `cube-sticky` 作为容器，`cube-sticky-ele` 作为需要吸顶的元素。每个 `cube-sticky-ele` 需要设置唯一的 `eleKey`。
<!-- @example: sticky-basic -->

</card>
